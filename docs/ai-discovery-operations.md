# AI Discovery and Citation Operations

Last checked: 2026-08-12

This document separates crawler access, search indexing, AI citations, and product-feed eligibility. None of these systems guarantees inclusion, ranking, citation, or a lead.

## Current foundation

| Surface | Current website control | Status |
| --- | --- | --- |
| ChatGPT Search | `OAI-SearchBot` in `robots.txt` | Explicitly allowed |
| Claude Search | `Claude-SearchBot` and `Claude-User` in `robots.txt` | Explicitly allowed |
| Perplexity | `PerplexityBot` and `Perplexity-User` in `robots.txt` | Explicitly allowed |
| Google AI features | Google Search crawl, index, and snippet eligibility | Allowed through the general crawler rule |
| Microsoft Copilot and Bing AI | Bing crawl/index eligibility | Allowed through the general crawler rule |
| Machine-readable references | `/llms.txt`, `/llms-full.txt`, `/agents.md`, `/product-catalog.json`, and `/feeds/products.tsv` | Published by the application |
| Search discovery | `/sitemap.xml` | Published by the application |

`llms.txt` and the public catalog are supplementary references. They do not replace crawlable visible pages, indexing, original content, external authority, or a reliable page response.

Official platform references:

- [OpenAI crawlers](https://developers.openai.com/api/docs/bots)
- [Google AI features and websites](https://developers.google.com/search/docs/appearance/ai-features)
- [Perplexity crawlers](https://docs.perplexity.ai/docs/resources/perplexity-crawlers)
- [Anthropic crawler controls](https://support.claude.com/en/articles/8896518-does-anthropic-crawl-data-from-the-web-and-how-can-site-owners-block-the-crawler)
- [Bing AI Performance](https://blogs.bing.com/webmaster/February-2026/Introducing-AI-Performance-in-Bing-Webmaster-Tools-Public-Preview)

## Crawl reliability evidence

The product routes are statically generated by Next.js and the observed apex homepage response was a Vercel cache hit behind Cloudflare. The `www` host returned a permanent redirect to the apex host.

The operator network also produced intermittent TLS failures and 12-second timeouts for product-page requests using OAI, Claude, Perplexity, Bing, and Google user agents. This does not prove a worldwide origin or crawler failure. Re-test with PageSpeed and independent regions before changing caching or CDN behavior.

An external search result checked on 2026-08-12 still showed an older homepage snapshot with retired products, old MOQs, and old geographic wording. Faster change notification and recrawl monitoring are therefore required.

## IndexNow

IndexNow notifies participating search engines that a URL changed. It does not guarantee a crawl or indexing.

The public ownership key is:

`https://universalpackaginggroup.com/75e2b2f10c82e360acc19a29a8670919.txt`

After the key file is deployed, submit every canonical URL from the live sitemap:

```bash
npm run indexnow:submit
```

Preview a submission without contacting IndexNow:

```bash
npm run indexnow:submit -- --dry-run --url / --url /products/custom-tuck-boxes
```

Submit one or more changed URLs after their production deployment is live:

```bash
npm run indexnow:submit -- --url /products/custom-tuck-boxes --url /faq
```

The script accepts only canonical URLs on `https://universalpackaginggroup.com`, verifies the published ownership key before a real submission, removes duplicates, and treats only HTTP 200 or 202 as accepted.

Official protocol reference: [IndexNow documentation](https://www.indexnow.org/documentation)

## Bing Webmaster setup

Completed on 2026-08-12:

- Imported only `https://universalpackaginggroup.com/` from the verified Google Search Console account. The two unrelated properties offered by the import flow were not selected.
- Confirmed `/sitemap.xml` has status `Success`, was last crawled on 2026-08-10, and contains 35 discovered URLs.
- Generated a Bing Webmaster API key and stored it in macOS Keychain under service `UPG Bing Webmaster API` and account `umar@universalpackaginggroup.com`. The credential must never be committed, printed, or placed in documentation.
- Verified the credential through `GetUserSites`; it returned the canonical UPG property as verified.
- Bing Search Performance showed 1 click and 22 impressions for the three-month view.
- Bing AI Performance showed 23 citations from Microsoft Copilots and partners across four cited pages in the three-month view: homepage 19, artwork-preparation guide 2, About page 1, and MOQ guide 1. The grounding-query sample was not yet available. Citation counts are discovery evidence, not rankings or lead attribution.

Next checks:

1. After release, verify that Bingbot can fetch the five product-family pages, both sample-kit pages, and the IndexNow ownership key.
2. Run the first IndexNow submission only after the ownership key and changed URLs are live.
3. Re-check URL Inspection, Site Explorer, Search Performance, Backlinks, Recommendations, and AI Performance after Bing processes the release.
4. Use the cited-page and grounding-query reports to choose content improvements when Bing exposes a sufficient sample.

## ChatGPT product discovery

OpenAI's Agentic Commerce Protocol can ingest structured product feeds for ChatGPT shopping experiences, but onboarding is currently limited to approved partners.

Only the two real fixed-price sample kits are candidates for an ACP product feed. Quote-led custom production pages must not be represented as fixed-price products. Do not start an ACP submission until sample-kit imagery, fulfillment, checkout, shipping, returns, and Merchant review are stable.

Official reference: [OpenAI Agentic Commerce onboarding](https://developers.openai.com/commerce/guides/get-started)

## Remaining citation work

1. Build the UPG Packaging Spec and MOQ Builder.
2. Add expert-reviewed, answer-first technical pages using UPG-approved product knowledge.
3. Publish original structure diagrams, comparison tables, and downloadable specification templates.
4. Earn third-party citations through packaging designers, branding agencies, ecommerce consultants, and relevant industry resources.
5. Maintain a fixed monthly prompt set for ChatGPT, Claude, Perplexity, Copilot, and Google AI; record mentions, citations, cited URLs, accuracy, referrals, and qualified leads.
